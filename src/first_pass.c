#include "assemble.h"
#include <stdlib.h>

#define MAX_LINE_LENGTH 256


void first_pass(FILE *input_file, int label_count, int max_label_length) {
    char line[MAX_LINE_LENGTH];
    for (fgets(line, MAX_LINE_LENGTH, input_file)) {
        while ()
    }
}